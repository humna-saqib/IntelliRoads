# RL Pipeline — Original Training Artifacts

This folder holds artifacts from the original 1,000-episode training run (single fixed traffic scenario, since superseded — see below).

Current artifacts:
- `training_results/episode_log.csv` — the original training log
- `training_graphs/reward_curve.png` — the original training reward curve

**For current documentation, results, and reproduction steps, see [`README_RL.md`](../README_RL.md) at the project root.** That covers the full picture: the original training run, the follow-up 300 epochs under randomized traffic demand, the validated evaluation results, and the statistical significance test.
