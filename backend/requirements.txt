fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
pydantic==2.5.0
python-dotenv==1.0.0
loguru==0.7.2
aiosqlite==0.19.0
sumolib
traci

# CPU-only PyTorch build for the DQN training pipeline (Sprint 2).
# Without this extra index, `pip install -r requirements.txt` on a
# machine without CUDA would still work, but a plain PyPI resolve can
# pull a much larger CUDA-enabled wheel — pin to the CPU index instead.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
numpy
matplotlib